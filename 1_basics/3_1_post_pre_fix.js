//post-increment(x++)

let x = 3;
const y = x++; // first it return the original value then , increase the value

console.log(`x:${x}, y:${y}`);


//pre-increment (++a)

let a = 3;
const b = ++a;
console.log(`a:${a}, b:${b}`); // first increase the value then use update value
